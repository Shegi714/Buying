SOURCE_SHEET_ID=
TARGET_SHEET_ID=

# Option A (recommended): put full service account JSON into GOOGLE_CREDS_JSON
# GOOGLE_CREDS_JSON={"type":"service_account", ...}

# Option B: path to local service account JSON file (never commit it)
GOOGLE_CREDS_PATH=./creds/service_account.json

